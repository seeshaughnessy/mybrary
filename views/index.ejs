<!-- Middle -->
<h2>Recently Added</h2>

<!-- Display Books -->
<div>
    <% books.forEach(book => { %>
        <a href="/books/<%= book.id %>">
            <img src="<%= book.coverImagePath %>" height="150" width="100">
        </a>
    <% }) %>
</div>